/**
 * eventListener.ts — Excel 事件监听
 *
 * 监听选择变更和内容变更，实现上下文感知联动。
 * Phase 4 实现完整的联动逻辑。
 */

/**
 * 注册 Excel 事件监听器
 * TODO: Phase 4 实现
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function registerEventListeners(
  _onSelectionChanged?: (rangeAddress: string) => void,
  _onChanged?: (rangeAddress: string) => void
): void {
  console.log('[eventListener] Registered (placeholder)');
}
